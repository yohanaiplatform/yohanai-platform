---
title: Project Status
description: Current development status of Yohan.AI Platform
---

# Project Status

> Dokumen ini selalu menggambarkan kondisi proyek saat ini dan diperbarui setiap selesai Sprint atau milestone besar.

---

# Current Version

**v0.1.0-alpha**

---

# Current Sprint

**Sprint 011**

---

# Current Phase

⏸️ **ON HOLD — Personal Use Phase** (sejak 23 September 2026)

Pengembangan SaaS dihentikan sementara. Platform diturunkan ke mode pemakaian personal dulu; pengembangan fitur SaaS dilanjutkan nanti setelah fase ini selesai.

Konsekuensi teknis (lihat bagian **Platform Lock** di bawah):

- Semua route publik diarahkan ke halaman `/under-development`
- Pendaftaran akun baru ditutup — di level aplikasi maupun di Supabase Auth
- Login tetap terbuka untuk akun yang sudah terdaftar (pemakaian personal)

Fase sebelumnya: 🚀 Feature Development (Sprint 011) — dilanjutkan saat lock dibuka.

---

# Audit Skema Database — 23 September 2026

Seluruh riwayat migration kini **utuh, `001`–`035`, tanpa celah**, dan terverifikasi sinkron antara repository dan database.

**Masalah yang dibereskan.** Perubahan skema Sprint 011 (Edit Profile dan Notification Preferences) dulu dijalankan langsung lewat Supabase SQL Editor dan tidak pernah tercatat sebagai migration maupun di-commit — database tidak bisa dibangun ulang dari nol. Seluruh objeknya ditarik ulang dari struktur database hidup menjadi migration `022`–`026`, ditulis idempotent, lalu diterapkan dengan `supabase db push`.

**Tiga temuan selama audit:**

1. **`public.profile_completeness_rules` punya RLS aktif tanpa satu pun policy.** Query langsung dari client di `EditProfileForm.tsx` selalu mengembalikan nol baris — daftar field wajib diam-diam kosong. Gejalanya menipu: progress bar tetap tampak benar karena dihitung RPC `check_profile_completeness()` yang `SECURITY DEFINER` sehingga menembus RLS. Diperbaiki di `025`, terverifikasi policy-nya kini ada.

2. **Tabel `customer.*` punya policy RLS tapi tanpa GRANT sama sekali.** Policy `authenticated_all` sudah benar, tapi role `authenticated` tidak pernah diberi hak akses dasar — query ditolak Postgres sebelum RLS sempat dievaluasi. Ini akan memblokir modul CRM dan integrasi lead intake. Diperbaiki di `027`.

3. **Hak tulis berlebih pada tabel referensi di schema `public`.** Supabase memberi GRANT ALL ke `anon` dan `authenticated` secara default, sehingga tabel wilayah dan `profile_completeness_rules` ikut membawa hak INSERT, UPDATE, DELETE, bahkan TRUNCATE yang tidak pernah diniatkan. Dicabut di `027`, disisakan SELECT untuk `authenticated` saja.

**Empat temuan lanjutan dari database linter Supabase** (`028`–`030`):

4. **`check_profile_completeness` bisa dipanggil tanpa login.** Fungsi `SECURITY DEFINER` ini terbuka untuk role `anon`, sehingga siapa pun tanpa akun bisa menanyakan kelengkapan profil user mana pun lewat REST. Ditutup di `028`.

5. **`search_path` empat fungsi `core` tidak dikunci**, termasuk `is_authenticated()` yang dipakai policy RLS di seluruh tabel domain. Dikunci di `028`, dan diverifikasi fungsinya tetap berjalan sehingga RLS tidak lumpuh.

6. **Mengunci EXECUTE ternyata butuh dua pencabutan.** Postgres memberi EXECUTE ke `PUBLIC` saat `CREATE FUNCTION`, sementara Supabase menambahkan grant eksplisit ke `anon` dan `authenticated`. Mencabut salah satunya saja menyisakan yang lain — kesalahan ini terjadi dua kali secara bergantian sebelum tertutup di `029`.

7. **Fungsi `SECURITY DEFINER` mempercayai parameter identitas dari client.** `check_profile_completeness` menerima `p_user_id` apa adanya, sehingga setelah celah `anon` ditutup pun, sesama user yang sudah login masih bisa saling mengintip kelengkapan profil. Diperbaiki di `030`: identitas kini diambil dari `auth.uid()` dan parameter yang menunjuk user lain ditolak.

**Yang sengaja dibiarkan:** EXECUTE `core.is_authenticated()` dari `PUBLIC` tidak dicabut — fungsi itu dievaluasi di dalam policy RLS memakai hak role pemanggil, sehingga mencabutnya melumpuhkan RLS di seluruh database tanpa imbalan keamanan apa pun. Lima ekstensi di schema `public` (`vector`, `pg_trgm`, `btree_gist`, `btree_gin`, `moddatetime`) juga dibiarkan karena memindahkannya berisiko memecah index dan tipe kolom yang memakainya.

**Pelajaran yang dicatat permanen di `AGENTS.md`:** isi repository bukan cermin isi database. Policy RLS untuk seluruh schema domain ternyata ada di database tapi tidak ada di `017_rls.sql` — kesimpulan soal keadaan database tidak boleh diambil hanya dari membaca file migration.

**Pola yang sama ketemu lagi 2x, kali ini bukan di `authenticated`+`customer` (`027`), tapi di kombinasi role/schema lain:**

8. **`service_role` tidak pernah diberi `GRANT USAGE` ke schema domain manapun** (`customer`, `core`, `chat`, `property`, `auth_ext`) — cuma otomatis dapat di `public`. Ketemu saat verifikasi runtime `POST /api/leads/intake` di production: `permission denied for schema customer`. `service_role` punya atribut `BYPASSRLS`, tapi itu cuma melewati evaluasi RLS policy, bukan GRANT dasar. Diperbaiki **cuma untuk schema `customer`** di `032` (scope sengaja dipersempit ke yang benar-benar dipakai hari ini).

9. **`authenticated` tidak pernah diberi GRANT ke `chat.conversations`/`chat.messages`.** Policy RLS `authenticated_all` sudah benar, tapi widget "Recent Chats" di dashboard selalu balas `Error: Unable to load conversations` karena GRANT dasarnya tidak ada. Diperbaiki di `034`.

**Audit menyeluruh dilakukan setelah temuan ke-2 ini** (23 September 2026) untuk memetakan celah yang tersisa, supaya sesi berikutnya tidak perlu menemukan ulang satu-satu. Hasilnya:

| Role | Schema tanpa `USAGE` |
|------|----------------------|
| `service_role` | `auth_ext`, `chat`, `core`, `property` (customer sudah diperbaiki di `032`) |
| `authenticated` / `anon` | Tidak ada — `USAGE` sudah ada di semua schema domain |

`USAGE` di level schema saja tidak cukup — perlu dicek juga per tabel. Tabel yang `authenticated` **belum** punya `SELECT`-nya (per 23 September 2026, sebelum ada kebutuhan nyata jadi sengaja belum diperbaiki):

- `core.audit_logs`, `core.permissions`, `core.role_permissions`, `core.roles`, `core.settings`
- `property.categories`, `property.listings`

**Implikasi untuk kerja berikutnya:** kalau mulai membangun modul yang menyentuh RBAC (`core.roles`/`permissions`) atau Property Module (`property.listings`), **cek dulu** `has_table_privilege(role, 'schema.table', 'SELECT')` sebelum menulis kode query-nya — jangan asumsikan GRANT otomatis ada hanya karena RLS policy-nya ada. `getPropertySummary.ts` saat ini murni stub (return angka 0 hardcode, parameter `supabase`-nya bahkan tidak dipakai) — bukan diblokir GRANT, memang belum pernah diimplementasikan.

**Catatan operasional:** perintah `GRANT` lewat Supabase MCP (`execute_sql`/`apply_migration`) **selalu ditahan otomatis** oleh classifier permission Claude Code ("Permission Grant"), butuh konfirmasi eksplisit dari Yohan di chat tiap kali — bukan sekali setting lalu selesai. Ini kejadian konsisten sepanjang sesi 23 September (migration `032` dan `034` sama-sama sempat tertahan dulu).

**10. `authenticated_all` di `customer.leads` berarti "semua akun yang login lihat SEMUA data" — bukan cuma milik sendiri.** Baru disadari 23 September 2026 (malam) saat Yohan sadar masih pakai `admin@yohanai.id` padahal ada akun kedua di sistem: `ramlan.hadiansyah@gmail.com` (rekan Yohan di Griya Indonesia, daftar 27 Agustus 2026 lewat Google OAuth, sebelum platform lock aktif). Dengan policy lama, akun itu otomatis bisa melihat semua 1968 lead. Diperbaiki di `035`:
   - Kolom baru `customer.leads.assigned_to` (kepemilikan CRM).
   - 1968 lead lama di-assign ke `admin@yohanai.id` (disepakati sebagai data milik bisnis/owner).
   - Ramlan diberi role `agent` (dari `core.roles`, sudah ada sejak awal tapi baru dipakai sekarang).
   - Fungsi `core.is_admin_or_above()` + policy baru `leads_owner_or_admin`: admin/super_admin lihat semua, role lain cuma lihat lead yang `assigned_to` dirinya.
   - Diverifikasi lewat simulasi JWT langsung di database (bukan cuma baca kode): admin lihat 1968, Ramlan lihat 0, Ramlan bisa insert lead miliknya sendiri, Ramlan ditolak insert lead atas nama admin.

**Keputusan yang masih ditunda (disengaja, bukan lupa):** `admin@yohanai.id` untuk sementara tetap dipakai Yohan sehari-hari (develop/testing dengan data asli) sekaligus berperan admin. Pemisahan akun developer murni vs akun bisnis Yohan yang sesungguhnya ditunda sampai strukturnya "firm" — jangan buru-buru memisahkan ini di sesi berikutnya tanpa dikonfirmasi ulang dulu.

---

# Platform Lock

Diaktifkan 23 September 2026 saat project masuk hold.

**Cara kerja:** gate di `src/lib/platform-lock.ts`, dipanggil dari `src/proxy.ts` lewat `updateSession()`. Default-nya **terkunci** — platform hanya terbuka kalau env var `PLATFORM_LOCKED` di-set eksplisit ke `false`, supaya deploy tanpa env var tidak diam-diam membuka pendaftaran publik lagi.

| Env var | Fungsi |
|---------|--------|
| `PLATFORM_LOCKED` | Sumber kebenaran, dibaca middleware. `false` = platform terbuka |
| `NEXT_PUBLIC_PLATFORM_LOCKED` | Flag UI saja (menyembunyikan link ke route mati). Set berbarengan |
| `PLATFORM_ALLOWED_EMAILS` | Daftar email yang boleh masuk selama terkunci, dipisah koma. Kosong = semua akun terdaftar boleh |

**Route yang tetap terbuka selama terkunci:** `/under-development`, `/login`, `/forgot-password`, `/reset-password`, `/callback` (OAuth Google harus bisa mendarat), `/privacy-policy` & `/data-deletion` (URL-nya terdaftar di Google OAuth consent screen), `/api/health`.

**Route yang diblokir total:** `/register` dan `/verify-email` — diblokir di proxy sekaligus di `page.tsx`-nya sebagai lapis kedua.

**Bug yang ketemu saat memasang lock — `middleware.ts` tidak pernah jalan.** File-nya ada di root project, padahal project ini memakai direktori `src/` sehingga Next mencarinya di `src/` (sejajar dengan `app/`). Next mengabaikannya tanpa peringatan apa pun. Terbukti dari dev log: `GET /` balas `200` bukan redirect, dan `/dashboard` juga `200` — yang membelokkan ke login selama ini cuma `AuthGuard` di sisi client. Artinya refresh sesi Supabase lewat middleware tidak pernah aktif sejak awal. Diperbaiki sekaligus ke konvensi Next 16: file dipindah ke `src/proxy.ts` dengan named export `proxy` (Next 16 mendeprecate nama `middleware` menjadi `proxy`; runtime-nya `nodejs`, bukan `edge`). Setelah pindah, gate terverifikasi jalan di dev.

**Penguncian di level Supabase:** toggle *Allow new users to sign up* di Supabase Authentication dimatikan. Ini yang benar-benar mengunci — blokir di aplikasi saja tidak cukup, karena `anon key` ada di bundle browser dan endpoint signup Supabase bisa dipanggil langsung tanpa lewat UI.

**Cara membuka kembali:** set `PLATFORM_LOCKED=false` + `NEXT_PUBLIC_PLATFORM_LOCKED=false` di Vercel, lalu nyalakan lagi toggle signup di Supabase. Tidak ada kode yang perlu diubah — landing page lama, halaman register, dan semua form-nya masih utuh di codebase.

---

# Overall Progress

**≈ 68%**

---

# Current Focus

## Priority 1

Authentication Stabilization — Selesai secara teknis (Google OAuth + Email/Password). Facebook Login dibatalkan permanen.

Target:

- Google OAuth Login & Register — ✅ Confirmed jalan di production
- Forgot Password — ✅ Fixed & confirmed (kini via Resend SMTP, sebelumnya Gmail App Password)
- Register (email) & Email Verification — ✅ Confirmed jalan end-to-end
- Profile Page dummy data — ✅ Fixed, fetch data real dari `auth_ext.profiles`
- **Facebook Login — ❌ Dibatalkan (bukan sekadar ditunda).** Meta menolak Business Verification karena dokumen legal usaha (NIB) terdaftar atas nama usaha pribadi Yohan, sedangkan "YohanAI" adalah brand yang berada di bawah naungan usaha tersebut — mismatch nama legal ini membuat verifikasi otomatis Meta terus gagal meski dokumen sudah benar formatnya. Diputuskan tidak worth effort untuk dikejar lebih lanjut; Google OAuth + Email/Password dianggap cukup untuk kebutuhan login user. Sudah dibersihkan penuh dari codebase: `FacebookButton.tsx` dihapus, referensi di `LoginForm.tsx` & `RegisterForm.tsx` dihapus, provider Facebook di Supabase Authentication dimatikan (toggle off).
- Privacy Policy & Data Deletion pages — ✅ Dibuat, sekarang juga mencantumkan nama legal usaha
- Email domain `@yohanai.id` — ✅ Fixed. Root cause: domain `yohanai.id` (DNS dikelola Cloudflare, terhubung ke Vercel untuk web) tidak punya **MX Record** sama sekali, sehingga semua email masuk ke `admin@yohanai.id` (termasuk kode verifikasi Meta) tidak pernah sampai. MX Record ditambahkan mengarah ke `pangulubao.iixcp.rumahweb.net`, terverifikasi jalan via MXToolbox dan email masuk sukses diterima.
- **SMTP outbound (auth emails)** — ✅ Migrasi dari Gmail personal ke **Resend**. Password SMTP `admin@yohanai.id` di cPanel juga sudah diperbaiki sebagai langkah awal sebelum migrasi. Domain `yohanai.id` diverifikasi di Resend dengan subdomain khusus `send.yohanai.id` (memakai MX & TXT terpisah), sehingga email masuk existing di `yohanai.id` (via rumahweb) tidak terganggu. Supabase Authentication → SMTP Settings sudah dikonfigurasi ke `smtp.resend.com` (port 465). Tested end-to-end via flow Forgot Password — email diterima langsung di Focused Inbox (bukan Spam) pada percobaan pertama.
- **SPF, DKIM, DMARC untuk `yohanai.id`** — ✅ Selesai. SPF & DKIM ter-setup otomatis lewat proses verifikasi domain Resend (`send.yohanai.id`). DMARC record (`_dmarc.yohanai.id`, `v=DMARC1; p=none`) ditambahkan manual di Cloudflare, mode monitoring (`p=none`) untuk tahap awal.
- **Supabase Free Tier auto-pause mitigation** — ✅ Selesai. Dibuat GitHub Actions scheduled workflow (`.github/workflows/supabase-keepalive.yml`) yang ping endpoint `/auth/v1/admin/users` (pakai `service_role` key) tiap 3 hari untuk reset timer inactivity Supabase (batas asli: 7 hari tanpa aktivitas query database). Workflow juga fail eksplisit + kirim notifikasi GitHub kalau ping gagal, bukan silent fail.

Semua menggunakan data Supabase.

---

## Priority 2

Edit Profile Feature — ✅ **Selesai.** Semua sub-task inti tuntas dan tervalidasi end-to-end. Sisa 2 item kecil (upload foto profil, keputusan System Information) dipindah ke backlog terpisah, tidak lagi memblokir milestone berikutnya.

Target:

- **Migration `001_edit_profile_schema.sql`** — ✅ Selesai (Bagian 1–5): `core.business_roles` (4 role), 4 tabel `wilayah_provinsi/kabupaten/kecamatan/desa`, kolom baru `auth_ext.profiles` (`business_role_id`, `address_line`, `wilayah_desa_id`, social media URLs, `role_details` JSONB), tabel `profile_completeness_rules` (table-driven, dengan UNIQUE constraint + dukungan conditional field), function RPC `check_profile_completeness()` (dengan `REVOKE`/`GRANT` eksplisit).
- **Seed data wilayah** — ✅ Selesai. Dataset `cahyadsn/wilayah` diparse & divalidasi silang dengan angka resmi (38 provinsi, 514 kab/kota, 7.285 kecamatan, 83.762 desa/kelurahan — cocok persis), diimport via CSV ke 4 tabel wilayah dengan 0 baris yatim (FK integrity bersih).
- **`WilayahSelector.tsx`** (cascading dropdown Provinsi → Kabupaten → Kecamatan → Desa) — ✅ Selesai & jalan. Query per-level (bukan fetch semua 83rb baris sekaligus), resolve mundur otomatis untuk mode edit, empty state eksplisit.
- **`EditProfileForm.tsx`** — ✅ Selesai & jalan. Field wajib dibaca dari `profile_completeness_rules` (table-driven, bukan hardcode), field kondisional (mis. No. Sertifikat Broker) muncul/hilang otomatis berdasar `depends_on_field_key`, progress bar Tier 1 & Tier 2 real-time dari RPC.
- **Debugging infrastruktur yang ditemukan selama development** — ✅ Selesai, 3 root cause tersolusi:
  1. Schema `auth_ext`, `core`, `customer`, `chat`, `property` belum ter-expose di Data API Settings (cuma `public` + `graphql_public` aktif) — sudah ditambahkan semua.
  2. `GRANT SELECT/UPDATE` belum diberikan ke role `authenticated` untuk `auth_ext.profiles` dan `core.business_roles` (RLS policy sudah benar, tapi grant dasar di bawahnya belum ada — dua lapisan berbeda di Postgres).
  3. CSS variable `--popover`/`--popover-foreground` tidak pernah didefinisikan di `tokens.css` project, menyebabkan semua dropdown dari komponen Select (shadcn/Base UI) tampil transparan/tembus pandang — sudah ditambahkan.
- **Change Password** — ✅ Selesai & tervalidasi end-to-end. Route baru `/change-password`, terpisah dari Forgot Password. Wajib re-autentikasi pakai password lama (`signInWithPassword`) sebelum boleh `updateUser` ke password baru — mencegah orang yang cuma mencuri session (tanpa tahu password asli) bisa ganti password dan mengunci pemilik akun keluar. Deteksi otomatis user yang daftar via Google OAuth (`user.identities`) — tidak ditampilkan form password sama sekali untuk akun Google-only, diganti pesan penjelasan. Toggle show/hide (icon mata) di ketiga field. Test akhir: ganti password lewat form, logout, login ulang pakai password baru — berhasil, konfirmasi perubahan benar-benar tersimpan di Supabase Auth, bukan cuma sukses di UI.
- **Notification Preferences** — ✅ Selesai & tervalidasi end-to-end. Route baru `/notification-preferences`. Tabel `auth_ext.notification_preferences` (flat per user, bukan table-driven — kategori notifikasi tetap/fixed, bukan dinamis per role), RLS + GRANT dibuat idempotent sejak awal (`DROP POLICY IF EXISTS` + `GRANT` eksplisit langsung di migration, menerapkan lesson learned dari Edit Profile tanpa perlu kena error dulu). 4 kategori toggle (New Lead Alert, Pesan/Chat, Update Properti — masing-masing Email & Dalam Aplikasi; Marketing/Promo — Email saja) plus 1 kategori "Keamanan Akun" yang sengaja **tidak** punya toggle (selalu aktif, tidak bisa dimatikan user). Upsert `onConflict: user_id` untuk baris yang belum pernah ada. Test akhir: matikan sebagian toggle, simpan, refresh halaman — nilai tetap tersimpan, konfirmasi data masuk database bukan cuma state React.
- **Twilio Verify (Phone provider)** — ⏸️ **Sengaja ditunda.** Yohan masih tahap trial/testing, belum siap upgrade ke akun Twilio berbayar. Konsekuensi: rule Tier 1 `phone_confirmed_at` akan selalu tampil "belum lengkap" sampai Twilio aktif — ini expected, bukan bug. Rencana resume saat sudah butuh volume user besar / mendekati go-live.

---

## Branding & Visual Identity (25 September 2026)

✅ **Selesai.** Logo `Yohan AI` di header mobile sebelumnya render gepeng/rusak — bukan sekadar soal kualitas gambar, tapi dua masalah terpisah:

- **Bug flexbox** — search bar di tengah header (`SearchCommand.tsx`) tidak punya `min-width:0`, jadi saat viewport sempit, flexbox malah menyusutkan grup logo+menu demi mempertahankan ukuran search bar. Diperbaiki: grup logo & grup notif/avatar diberi `shrink-0`, wrapper search diberi `min-w-0`.
- **Aset logo boros ruang** — `logo-light.png`/`logo-dark.png` yang lama cuma terisi 24% dari kanvasnya (diukur pakai `sharp`, bukan cuma lihat mata). Diganti dengan versi crop rapat (53–65% terisi) dari folder Logos Yohan di Google Drive, badge "AI" juga diganti dari gaya glossy/gradient 3D ke flat outline merah (kontras lebih baik, terutama di dark mode).
- **`apple-touch-icon.png.png`** (dobel ekstensi) — ikon "Add to Home Screen" di iPhone 404 diam-diam selama ini. File di-rename.
- **Boot screen** (`AuthGuard.tsx`, tampil pertama kali buka app sebelum dashboard render) — diganti dari spinner generik abu-abu jadi app-icon dengan animasi pulse + 3 titik loading merah, brand-forward.

Semua diverifikasi visual di panel browser (mobile 375px, tablet, desktop) sebelum & sesudah. `logo-landscape.png` (dead code, tidak dipakai di manapun) belum disentuh — bukan urgent.

---

## Backlog (tidak memblokir milestone berikutnya)

- **Upload foto profil (kamera/galeri/Google Drive)** — ⏳ Belum dikerjakan. Saat ini `avatar_url` masih berupa input teks URL manual. Butuh Supabase Storage bucket + policy + integrasi picker.
- **Keputusan "System Information" di Profile page** — ⏳ Perlu keputusan Yohan: hapus total, atau sembunyikan kondisional (cuma tampil untuk `role_id` admin/super_admin). Saat ini tampil ke semua user termasuk detail environment/build — berpotensi bocor info internal.

---

## Next Priority

- **Lead Intake Fase 1 — ✅ Selesai & terverifikasi end-to-end di production (23 Sep 2026).** Endpoint, migration, dedup, normalisasi HP semua tes lolos lewat `curl` langsung ke `https://yohanai.id`. Backfill ~2000 lead lama sedang/sudah berjalan lewat Apps Script (`backfillLeadsToYohanAI()`, dijalankan manual oleh Yohan). Detail lengkap di `docs/modules/crm.mdx`.
- **CRM Foundation Fase 2 — ✅ Selesai (25 Sep 2026).** Lead List, Lead Detail, ubah status dari UI, assign lead ke agent, filter Sumber Informasi & Kategori, tombol WA, Lead Funnel nyata & bisa diklik, AI Intelligence sebagian nyata — semua live. Detail lengkap di `docs/modules/crm.mdx`.
- Property Module
- Sales Pipeline
- AI Intelligence (lanjutan — 1 insight nyata sudah ada, WAHA/Fonnte belum diputuskan)
- Communication Automation (blocked oleh keputusan WAHA vs Fonnte)

---

# Technical Health

| Area | Status |
|------|--------|
| Architecture | 🟢 Excellent |
| TypeScript | 🟢 PASS |
| ESLint | 🟢 PASS |
| Production Build | 🟢 PASS |
| Deployment | 🟢 PASS |
| Technical Debt | 🟢 Low |

---

# Infrastructure

| Component | Status |
|-----------|--------|
| GitHub | ✅ (termasuk scheduled workflow Supabase keep-alive) |
| Supabase | ✅ (Free tier — auto-pause dimitigasi via GitHub Actions ping tiap 3 hari) |
| Vercel | ✅ |
| Mintlify | ✅ |
| Cloudflare DNS | ✅ (MX Record `@yohanai.id`, SPF, DKIM, DMARC semua aktif) |
| Resend (SMTP outbound) | ✅ Domain `yohanai.id` (subdomain `send.yohanai.id`) verified, terhubung ke Supabase Auth SMTP |
| Meta / Facebook App | ⚫ Tidak digunakan — Facebook Login dibatalkan permanen |
| Supabase Data API — Exposed Schemas | ✅ `public`, `graphql_public`, `auth_ext`, `core`, `customer`, `chat`, `property` (7 dari 12) |
| Twilio Verify (Phone provider) | ⏸️ Belum di-setup, sengaja ditunda sampai butuh volume user besar |

---

# Current Modules

| Module | Status |
|---------|--------|
| Landing Page | ✅ Complete |
| Authentication | 🟢 Stable secara teknis (Google, Email/Password, Forgot Password, Email Verification semua confirmed jalan; SMTP kini via Resend; Facebook Login dibatalkan permanen) |
| Profile | ✅ Edit Profile, Change Password, dan Notification Preferences semua confirmed jalan end-to-end. Backlog kecil tersisa: upload foto profil masih URL manual, keputusan visibility "System Information" |
| Dashboard Foundation | ✅ Complete — Lead Funnel & Recent Leads pakai data asli, 1 AI Insight nyata |
| CRM | ✅ Foundation Fase 2 selesai — Lead List, Lead Detail, ubah status, assign agent, filter, tambah lead manual semua live |
| Property | 🟡 In Progress — masih placeholder murni, belum ada satu baris `property.listings` |
| Sales | 🟡 Planned |
| AI Intelligence | 🟡 In Progress — 1 insight nyata (Follow-up Backlog), 2 kartu contoh ditandai placeholder |
| Communication Automation | 🔵 Planned — menunggu keputusan WAHA vs Fonnte |

---

# Open Issues

## High

- None

## Medium

- Supabase masih di Free tier — auto-pause dimitigasi via GitHub Actions, tapi tetap perlu dipantau (cek log workflow secara berkala); pertimbangkan upgrade plan kalau traffic mulai signifikan
- Twilio Verify belum di-setup — semua user akan selalu tampil "belum lengkap" di Tier 1 completeness sampai ini aktif; sengaja ditunda, bukan lupa

## Low

- 2 dari 3 kartu AI Intelligence masih contoh ilustratif (ditandai jelas "Segera hadir" di UI, tidak dihapus supaya gampang dikembalikan begitu AI Foundation jalan) — lihat `docs/modules/crm.mdx`
- Property Summary masih placeholder murni (`getPropertySummary.ts` return 0 hardcode, belum query `property.listings` sama sekali)
- `authenticated` belum punya `SELECT` di `property.listings`/`property.categories` dan beberapa tabel `core.*` (RBAC) — belum jadi masalah karena belum ada kode yang menyentuhnya, tapi cek dulu sebelum mulai Property Module (lihat bagian Audit Skema Database)
- Upload foto profil masih input URL manual, belum ada file upload (kamera/galeri/Drive)
- "System Information" di Profile page masih tampil ke semua user, termasuk detail environment/build — perlu keputusan hapus atau gating role admin
- OAuth consent screen Google masih menampilkan nama domain Supabase, bukan branding "YohanAI" (kosmetik, prioritas rendah)
- Security Centre Meta memberi rekomendasi (bukan wajib): 1 user dengan public email domain di Business Manager — opsional untuk ditindaklanjuti

---

# Next Milestone

**CRM Foundation — ✅ Selesai (25 September 2026).** Lead List, Lead Detail, ubah status dari UI, assign lead ke agent, dan filter Sumber Informasi semua live di production.

Berikutnya (belum dimulai, urutan sesuai `AGENTS.md`): keputusan WAHA vs Fonnte untuk Communication Automation (perlu dibahas eksplisit bareng Yohan sebelum mulai bangun integrasi WA apa pun), lalu Property Module, lalu lanjutan AI Foundation.

---

# Build Checklist

Sebelum merge:

- [x] TypeScript PASS
- [x] ESLint PASS
- [x] Build PASS
- [x] Runtime QA PASS
- [ ] Review PASS
- [ ] Git Commit
- [ ] Git Push
- [ ] Deployment PASS

---
## Active Sprint

Sprint 011

### Current Task

Task 001

Authentication Stabilization — ✅ Selesai (Google OAuth + Email/Password; Facebook Login dibatalkan permanen)

Task 002

Edit Profile Feature — ✅ **Selesai.** Role bisnis, data dasar, cascading wilayah, completeness table-driven, Change Password, Notification Preferences — semua tervalidasi end-to-end. Upload foto profil & keputusan System Information dipindah ke backlog terpisah.

Task 003

Lead Intake Fase 1 — ✅ **Selesai & terverifikasi end-to-end di production.** Endpoint `POST /api/leads/intake`, migration `031`-`034`, dedup, normalisasi HP, backfill data lama (Apps Script, dijalankan Yohan). Detail: `docs/modules/crm.mdx`.

Task 004

CRM Foundation Fase 2 — ✅ **Selesai.** Lead List, Lead Detail, ubah status dari UI, assign lead ke agent (migration `036`), filter Sumber Informasi. Detail: `docs/modules/crm.mdx`.

Task 005

Tambah Lead Manual — ✅ **Selesai (26 September 2026).** Tombol "Add Lead" di dashboard ternyata cuma link ke `/crm`, belum pernah dibangun jadi form beneran. Halaman `/crm/new` dibuat, migration `037` (sumber lead "Input Manual"). Detail: `docs/modules/crm.mdx` bagian "Tambah Lead Manual".

### Next Task

Task 006

Keputusan WAHA vs Fonnte untuk Communication Automation — belum dimulai, perlu dibahas eksplisit bareng Yohan dulu sebelum ada kode integrasi WA apa pun.

---

# Last Updated

26 September 2026

---

# Notes

Dokumen ini menggantikan:

- MASTER_PROGRESS
- DAILY_LOG
- AI_NEXT_ACTION
- AI_CONTEXT

Status proyek harus diperbarui setiap selesai Sprint atau milestone penting, bukan setiap percakapan.