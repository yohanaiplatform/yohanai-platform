---
title: CRM Module
description: Customer Relationship Management Module
---

# CRM Module

CRM merupakan inti operasional Yohan.AI.

Seluruh data calon pembeli, aktivitas follow-up, timeline komunikasi, dan AI Intelligence akan berpusat pada modul ini.

CRM dirancang sebagai **AI-assisted CRM**, bukan sekadar penyimpanan data pelanggan.

---

# Current Status

| Item | Status |
|------|--------|
| Development | ✅ Lead Intake Fase 1 & CRM Foundation Fase 2 selesai |
| Production Ready | ✅ Lead List, Lead Detail, ubah status, assign agent, filter, tambah/edit lead manual, catatan berwaktu, export CSV semua live |
| Skema database | ✅ Ada (`customer.leads`, `lead_sources`, `contacts`, `notes` sejak `038`) |
| RLS policy | ✅ Ada (`leads_owner_or_admin`/`notes_owner_or_admin` sejak `035`/`038`, sebelumnya `authenticated_all`) |
| GRANT | ✅ Ada untuk `authenticated` & `service_role` di schema `customer` (`027`, `032`, `038`) |
| UI | ✅ Lead List, Lead Detail, ubah status, assign agent, filter, tambah/edit lead manual, catatan, export CSV selesai |
| AI Integration | 🟡 1 insight nyata (Follow-up Backlog) di dashboard, sisanya masih Planned -- lihat catatan kecukupan data di bawah |
| Jalur masuk lead | Google Form (otomatis, Fase 1) **dan** input manual dari dashboard (`/crm/new`, 26 September 2026) -- satu arah, **tidak** sinkron balik ke spreadsheet |
| Bahasa | 🟡 Bi-lingual ID/EN sejak 26 September 2026 -- toggle di header, baru cakupan modul CRM, modul lain masih Indonesia tetap |

**Lapisan data sudah siap pakai.** Migration `007_customer_tables.sql` membuat `customer.leads` lengkap dengan pipeline status (`new` → `contacted` → `qualified` → `proposal` → `negotiation` → `won` → `lost`), `customer.lead_sources` (terisi 8 baris: Facebook, Instagram, TikTok, WhatsApp, Website, Referral, Google Ads, Organic Search), dan `customer.contacts`.

Sampai 23 September 2026 ketiga tabel itu punya policy RLS tapi **tanpa GRANT sama sekali**, sehingga query dari aplikasi selalu ditolak Postgres sebelum RLS sempat dievaluasi. Sudah diperbaiki di `027_fix_missing_grants.sql` — `leads` dan `contacts` kini bisa dibaca dan ditulis oleh user yang login, `lead_sources` hanya dibaca.

Yang tersisa murni pekerjaan aplikasi: jalur masuk data dan antarmukanya.

---

# Rencana Lead Intake dari Google Form

Dibahas 23 September 2026. **Fase 1 selesai dan terverifikasi end-to-end di production.**

## Status Fase 1 — 23 September 2026

Sudah selesai di sisi platform:

- **Audit Apps Script & spreadsheet.** 22 file `.gs`/`.html` diperiksa lewat editor Apps Script langsung (bukan cuma nama file). Ketemu 8 trigger aktif: 2 terikat ke "On form submit" (`onFormSubmitFormatTanggal` — sekadar format tampilan tanggal, dan `onFormSubmit_InputData` — bagian dari alur native Form-ke-Sheet), 6 sisanya time-based untuk AI Processor/Decision Engine/Property Matching Engine.
- **Peta kolom "Input Data"**: 52 kolom (A–AZ). Hanya A–Q yang data mentah (input form + turunan sederhana seperti Customer ID dan Link WA). R sampai AZ seluruhnya output AI Processor/Decision Engine/Property Matching Engine (AI_Intent, Budget Min/Max, Property Match Score, Lead Score, Priority Rank, Match Reasoning, dll) — **sengaja tidak ikut dikirim ke `customer.leads`**, sesuai peringatan di `docs/migration/migration-blueprint.mdx`.
- **Migration `031_add_google_form_lead_source.sql`** — baris `Google Form` ditambahkan ke `customer.lead_sources`, diterapkan ke database.
- **Route `POST /api/leads/intake`** (`src/app/api/leads/intake/route.ts`) — terima JSON dari Apps Script, validasi field wajib (`nama`, `phone`, `sourceRowRef`), dedup lewat `metadata->>source_row_ref`, insert ke `customer.leads` pakai service-role client (`src/lib/supabase/admin.ts`), field non-standar (kategori, permintaan, komentar, status funnel awal, dll) masuk ke `metadata` JSONB.
- **`/api/leads/intake` ditambahkan ke `PUBLIC_PATHS`** di `src/lib/platform-lock.ts` — diamankan lewat secret header (`LEADS_INTAKE_SECRET`), bukan sesi login.
- TypeScript PASS, ESLint tidak nambah error, `npm run build` PASS, guard 401/400 route sudah dites jalan di dev server.

**Endpoint sudah diverifikasi hidup di production** (24 September 2026): `SUPABASE_SERVICE_ROLE_KEY` dan `LEADS_INTAKE_SECRET` sudah terisi di Vercel, dan tes end-to-end lewat `curl` ke `https://yohanai.id/api/leads/intake` sukses — insert masuk ke `customer.leads`, dedup lewat `metadata->>source_row_ref` jalan (kirim payload sama dua kali → row kedua ditandai `duplicate: true` dengan `leadId` yang sama), `lead_source_id` benar mengarah ke "Google Form", nomor HP ternormalisasi ke format `62...`. Baris uji sudah dihapus lagi dari database.

Sempat ketemu bug tambahan saat verifikasi ini: `service_role` ternyata belum pernah diberi `GRANT USAGE` ke schema `customer` (baru ketahuan sekarang karena baru sekarang ada fitur pertama yang pakai service-role key) — endpoint sempat balas `permission denied for schema customer`. Diperbaiki di migration `032_grant_service_role_customer_schema.sql`. Detail pola masalahnya sudah dicatat di `AGENTS.md` bagian jebakan.

**Status per 23 September 2026, malam:** kurir Apps Script (`YohanAI_LeadIntake.gs`) sudah ditempel Yohan, trigger "On form submit" sudah terpasang (trigger tambahan, tidak mengubah/menghapus 8 trigger legacy yang sudah ada), kolom `Synced At` & `YohanAI Lead ID` sudah ditambahkan di sheet "Input Data". Backfill `backfillLeadsToYohanAI()` sudah/sedang dijalankan Yohan berkali-kali (dibatasi ~6 menit per run oleh Apps Script, jadi wajar butuh beberapa kali klik Run) — total lead di spreadsheet ±1968, per pengecekan terakhir di database sudah masuk 1141+ dan terus bertambah.

**Kalau mulai sesi baru dan mau tahu progres backfill saat ini**, cek langsung:

```sql
select count(*) from customer.leads where metadata->>'origin' = 'google_form_legacy';
```

Bandingkan dengan total di spreadsheet (kartu "Total" di dashboard Apps Script lama). Kalau sudah sama-sama ~1968, backfill selesai — jalankan ulang query di migration `033` sekali lagi (idempotent, aman diulang) untuk membenahi `created_at` baris-baris yang masuk di jeda waktu sebelum perbaikan `route.ts` ter-deploy:

```sql
UPDATE customer.leads
SET created_at = (regexp_replace(metadata->>'submitted_at', ' GMT.*$', '')::timestamp AT TIME ZONE 'Asia/Jakarta')
WHERE metadata->>'origin' = 'google_form_legacy'
  AND metadata->>'submitted_at' ~ '^[A-Za-z]{3} [A-Za-z]{3} \d{2} \d{4} \d{2}:\d{2}:\d{2} GMT'
  AND created_at::date = now()::date;
```

(Filter `created_at::date = now()::date` sengaja dibatasi ke baris yang masih "hari ini" — supaya tidak menyentuh baris yang sudah benar.)

## Temuan sampingan (di luar cakupan Fase 1, dicatat saja)

- **2 trigger AI error rate 100%**: `batchDetectConsumerIntent` dan `batchExtractBudget` di project Apps Script "CRM Yohan.AI" sudah gagal terus sejak beberapa waktu. Kolom `AI_Intent`, `Budget Min`, `Budget Max` di spreadsheet kemungkinan besar berhenti terisi karena ini.
- **API key AI dalam bentuk plaintext** di `YohanAI_Automation.gs` (`AUTO_CONFIG.AI_API_KEY`, endpoint `ai.flaz.id`). Bukan masalah platform Next.js, tapi worth rotate kalau ada waktu.

---

# Fase 2 — CRM Foundation (23-25 September 2026)

Dikerjakan langsung setelah Fase 1 live, berdasarkan feedback Yohan memakai halaman `/crm` dan `/dashboard` yang sudah production. **✅ Selesai (25 September 2026)** — baca/filter/kontak, Lead Detail, ubah status, dan assign agent semua sudah live.

## Yang sudah selesai

**Lead List (`/crm`)** — server component, bukan lagi placeholder:

- `src/lib/crm/getLeads.ts` — query `customer.leads` + join manual ke `customer.lead_sources` (dua query paralel, bukan PostgREST embed, supaya typing simpel), pagination lewat `range()`.
- `src/components/crm/LeadList.tsx` — tabel: Nama, Kontak, Sumber (dari `metadata->>sumber_informasi`, BUKAN dari `lead_sources.name` — semua lead Fase 1 sama-sama "Google Form" jadi tidak informatif), Kategori, Status, Tanggal Masuk, Aksi (tombol WA).
- `src/components/crm/lead-status-badge.tsx` — badge warna per status pipeline (`new`/`contacted`/.../`won`/`lost`).
- Filter (`src/components/crm/LeadListFilters.tsx`): rentang tanggal (`dateFrom`/`dateTo`) dan Kategori/Property (dropdown dari `src/constants/crm.ts`, hardcode dari opsi Google Form asli — belum ada tabel property yang jadi sumber kebenaran). Form native `method="GET"`, tanpa JavaScript, konsisten dengan pola server component di seluruh halaman ini.
- Filter `temperature` (hot/warm/cold/closing/batal, bisa multi-value dipisah koma mis. `hot,warm`) — dipicu dari klik tile Lead Funnel di dashboard, ditampilkan sebagai chip "Filter aktif" dengan link "Hapus filter".
- **Tombol WhatsApp** (`src/components/shared/WhatsAppButton.tsx`) — `wa.me/<phone>` + pesan pembuka otomatis, dipakai di Lead List DAN di Recent Leads dashboard (komponen bersama, bukan duplikat).

**Lead Detail (`/crm/[id]`)** — 24 September 2026, server component:

- `src/lib/crm/getLeadById.ts` — query satu lead lewat session client (RLS-scoped, bukan service-role, pola sama seperti `getLeads.ts`), join manual ke `lead_sources`.
- `src/app/(dashboard)/crm/[id]/page.tsx` — validasi bentuk UUID dulu sebelum query (id rusak di URL balas 404 lewat `notFound()`, bukan salah ditampilkan sebagai error database). Menampilkan status, kontak, sumber, kategori, temperature awal, hasil survey, follow-up terakhir, tanggal submit form, terakhir diperbarui, dan detail permintaan (minat unit/lokasi, permintaan, komentar) dari `metadata`. Tombol WA dan link kembali ke Lead List.
- `follow_up_terakhir` dan `submitted_at` di `metadata` tersimpan mentah sebagai `String(dateObject)` dari Apps Script (bukan ISO) — diparse ke tampilan tanggal Indonesia di halaman ini, pola sama seperti parsing `submitted_at` di `route.ts` untuk `created_at`.
- Nama lead di Lead List sekarang jadi link ke halaman ini.
- Diverifikasi end-to-end di dev server pakai data production asli (login `admin@yohanai.id` lewat panel browser Claude Code, klik lead dari Lead List, cek semua field terisi benar + tombol WA jalan).

**Ketemu saat kerjakan ini:** tipe `customer.leads` di `src/types/database.ts` belum pernah di-regenerate sejak migration `035` menambah kolom `assigned_to` — isi repo lagi-lagi tidak cermin isi database (pola yang sama seperti temuan RLS policy sebelumnya). Diverifikasi langsung ke database (`information_schema.columns`), lalu ditambal manual di tipe-nya.

**Ubah Status & Assign Agent (`/crm/[id]`)** — 25 September 2026:

- `src/components/crm/LeadStatusSelect.tsx` — dropdown status pipeline di Lead Detail, update langsung ke `customer.leads` lewat client Supabase browser (bukan Server Action — project belum pakai pola itu, konsisten dengan `NotificationPreferencesForm.tsx`). `LEAD_STATUS_CONFIG` di `lead-status-badge.tsx` di-export supaya daftar status cuma didefinisikan satu tempat.
- `src/components/crm/LeadAssignSelect.tsx` — dropdown "Ditugaskan ke", isinya dari fungsi baru `core.list_assignable_users()` (migration `036`). RLS `auth_ext.profiles` cuma izinkan user baca profil sendiri, jadi admin tidak bisa query daftar agent lewat cara biasa — perlu fungsi `SECURITY DEFINER` (pola sama seperti `core.is_admin_or_above()` di `035`) yang sengaja **return kosong** (bukan raise exception) kalau pemanggil bukan admin/super_admin. UI pakai sinyal itu: array kosong → tampilkan teks statis "Anda" (dijamin benar oleh RLS `leads_owner_or_admin` — non-admin cuma pernah lihat lead miliknya sendiri), array berisi → tampilkan dropdown beneran.
- Migration `036`: `REVOKE ALL FROM PUBLIC/anon`, `GRANT EXECUTE TO authenticated`, identitas dari `auth.uid()` bukan parameter client — pola yang sama persis dicatat di `AGENTS.md` supaya tidak diulang salah lagi.
- Diverifikasi 2 lapis: (1) simulasi JWT langsung di database (`SET LOCAL role authenticated` + `request.jwt.claims`) — admin dapat 2 baris, agent (Ramlan) dapat 0 baris; (2) end-to-end lewat browser sebagai admin — ubah status Pak Ngurah New→Contacted dan assignment-nya ke Ramlan, dicek langsung ke database (bukan cuma percaya state React), lalu data uji dikembalikan ke semula. Perilaku non-admin belum dites lewat browser langsung (tidak ada kredensial akun Ramlan), cuma diverifikasi di level database.

**Filter Sumber Informasi (`/crm`)** — 25 September 2026:

- `LEAD_SUMBER_OPTIONS` baru di `src/constants/crm.ts` — 5 nilai bersih dari data asli (Iklan Meta/Google, Ketemu di Lokasi, Dari Teman/Saudara/Referal, Lihat Baliho, Lainnya), pola sama seperti `LEAD_KATEGORI_OPTIONS`.
- Dropdown baru di `LeadListFilters.tsx`, filter di `getLeads.ts` (`metadata->>sumber_informasi`), dan `LeadListPagination.tsx` ikut membawa parameter `sumber` saat pindah halaman.
- Diverifikasi: filter "Ketemu di Lokasi" balas 152 lead, cocok persis dengan hitungan langsung ke database.

**Dashboard, tiga perbaikan:**

1. **Lead Funnel dulu selalu 0% semua temperature** — bug: query `getLeadSummary.ts` memfilter `leads.status = 'hot'/'warm'/'cold'`, padahal kolom itu pipeline (`new`/.../`won`/`lost`), CHECK constraint-nya bahkan menolak nilai itu. Temperature asli ada di `metadata->>status_funnel_awal`. Diperbaiki, DAN diperluas dari 3 jadi 5 kategori (Hot/Warm/Cold/Closing/Batal, sesuai 5 status funnel di spreadsheet legacy). Tiap tile sekarang **link ke `/crm?temperature=<x>`** dan dipindah ke atas halaman (sebelum Recent Leads/Chats).
2. **Recent Leads** — baris "Source ID: `<uuid>`" (tidak berguna) diganti Kategori lead; badge status "new" (selalu sama, tidak informatif — namanya "Recent" pasti baru) diganti tombol WA.
3. **AI Intelligence** — sebelumnya 100% teks statis/fiktif ("Lead #8821" tidak pernah ada). Diganti **sementara** (bukan dihapus): 1 kartu nyata "Follow-up Backlog" (`src/lib/dashboard/getDashboardInsights.ts`) menghitung lead Hot/Warm yang `metadata->>follow_up_terakhir`-nya kosong atau >48 jam, klik → `/crm?temperature=hot,warm`. 2 kartu lain **tetap ada di kode**, ditandai `isPlaceholder: true` + prefix "(Contoh)" + badge "Segera hadir" di UI — supaya gampang dikembalikan (cabut flag-nya) begitu AI Foundation (page-view tracking, dll) beneran dibangun.

**Bug data terkait, ditemukan & diperbaiki di jalan:**

- **`created_at` semua lead backfill = waktu insert (hari backfill jalan), bukan tanggal submit asli** — bikin "Today's New Leads" di dashboard selalu sama dengan Total Leads, dan filter tanggal di Lead List tidak berguna. `route.ts` sekarang parse `metadata.submittedAt` (format `Date.toString()` V8 dari Apps Script) jadi `created_at` eksplisit saat insert. Migration `033` membenahi retroaktif lead yang sudah kadung masuk. **Sengaja pakai `submitted_at` (tanggal submit form), bukan `follow_up_terakhir`** (tanggal follow-up terakhir/berikutnya) — dua konsep berbeda, jangan disamakan lagi di masa depan.
- **`authenticated` tidak pernah di-GRANT ke `chat.conversations`/`chat.messages`** — ketemu saat investigasi kenapa "Recent Chats" selalu `Error`. Pola sama persis dengan bug GRANT `service_role`×`customer` yang ditemukan di Fase 1. Diperbaiki di migration `034`. Ini **bukan** integrasi WhatsApp — cuma benerin izin baca tabel kosong, sekarang tampil "No Chats" (jujur) alih-alih Error.

## Migration baru sesi ini

| # | Isi |
|---|-----|
| `031` | Tambah "Google Form" ke `customer.lead_sources` |
| `032` | `GRANT USAGE` + hak tabel `customer.leads`/`lead_sources` ke `service_role` |
| `033` | Backfill `created_at` dari `metadata->>submitted_at` untuk lead Google Form |
| `034` | `GRANT SELECT/INSERT/UPDATE` `chat.conversations`/`chat.messages` ke `authenticated` |
| `035` | Pemisahan akses lead per akun: kolom `assigned_to`, role `agent` untuk Ramlan, policy `leads_owner_or_admin` (admin lihat semua, role lain cuma lihat miliknya sendiri) — lihat `docs/status.mdx` audit poin 10 |
| `036` | Fungsi `core.list_assignable_users()` — daftar user untuk dropdown assign lead, khusus admin/super_admin |
| `037` | Baris baru "Input Manual" di `customer.lead_sources`, untuk lead yang diinput langsung dari dashboard |
| `038` | Tabel `customer.notes` — catatan berwaktu per lead (append-only), policy `notes_owner_or_admin` |

## CRM Foundation Fase 2 — selesai

Semua item di checklist awal Fase 2 sudah live: Lead List (baca/filter/WA), Lead Detail, ubah status dari UI, assign lead ke agent, dan filter Sumber Informasi.

**Belum dikerjakan (di luar cakupan Fase 2, fase berikutnya):**

- **Timeline/Activity lengkap** — Notes (catatan berwaktu) sudah ada sejak 26 September 2026 (lihat bagian di bawah), tapi Activity terstruktur (Call/Meeting/Visit sebagai tipe tersendiri, bukan cuma teks bebas) belum. Ini "Phase 2"/"Phase 3" di roadmap CRM, bukan bagian dari CRM *Foundation*.
- **UI bulk-assign** di Lead List (assign banyak lead sekaligus) — sekarang cuma bisa satu-satu dari Lead Detail. Belum jadi kebutuhan mendesak karena baru ada 2 akun.

## Tambah Lead Manual (26 September 2026)

Ditanya Yohan setelah sadar tombol "Add Lead" di dashboard ternyata cuma link ke `/crm`, tidak benar-benar membuka form — kelihatan hidup padahal belum pernah dibangun. **Ini bukan pengganti Google Form** (lihat prinsip di bawah), melainkan jalur tambahan untuk lead yang datang di luar Google Form: telepon langsung, walk-in, dsb.

- Halaman `/crm/new` (`AddLeadForm.tsx`) — field & pilihan dropdown sengaja dibuat sama seperti Google Form (Sumber Informasi, Kategori/Property, Temperature Awal, Sudah Survey), supaya data yang masuk lewat dua jalur ini tetap konsisten bentuknya.
- `src/lib/crm/createLead.ts` — insert ke `customer.leads`, bentuk `metadata` dibuat identik dengan `POST /api/leads/intake` (cuma beda `origin`: `manual_dashboard` vs `google_form_legacy`) supaya Lead Detail tidak perlu cabang kasus khusus.
- Assign: non-admin otomatis ke diri sendiri (RLS `leads_owner_or_admin` cuma izinkan itu), admin bisa pilih siapa saja lewat `core.list_assignable_users()`.
- Tombol "Add Lead" (dashboard, Quick Actions) dan "+ Tambah Lead" (halaman `/crm`) sekarang mengarah ke sini.
- **Bug ketemu saat tes manual:** dropdown "Ditugaskan ke" sempat menampilkan UUID mentah alih-alih nama — komponen `Select` (Base UI) tidak otomatis me-resolve label dari `SelectItem` yang value-nya berbeda dari teks tampilan (di sini value = UUID, label = nama). Harus dikasih `children` eksplisit di `SelectValue`, pola yang sebenarnya sudah dipakai `WilayahSelector.tsx` tapi luput di-follow di komponen baru ini. **Kalau bikin dropdown baru yang value-nya bukan teks yang sama dengan label (mis. ID/UUID), selalu hitung label-nya sendiri dan taruh sebagai children `SelectValue`, jangan andalkan default rendering.**
- Diverifikasi end-to-end dengan data production asli: isi form lengkap, submit, redirect ke Lead Detail, semua field (termasuk normalisasi nomor HP) cocok persis dengan input, dicek langsung ke database. Data uji dihapus lagi setelahnya.

**Agent AI dari WhatsApp yang bisa input lead otomatis (permintaan Yohan yang sama) belum dibangun** — itu bergantung keputusan WAHA vs Fonnte yang belum diambil. Struktur `createLead.ts` dibuat generik supaya nanti bisa dipakai ulang oleh jalur itu juga, bukan eksklusif untuk form manual.

## Edit Lead & Catatan Berwaktu (26 September 2026)

Ditanya Yohan: field seperti "Sudah Survey" cuma bisa dilihat, padahal kondisinya berubah seiring follow-up (mis. awalnya "Belum", setelah follow-up ketiga jadi "Sudah"). Juga minta jejak waktu tiap ada update penting ("Lead sudah dapat unit di lokasi lain", "Lead batal booking", dst) — bukan cuma field yang di-overwrite dan riwayatnya hilang.

Dua mekanisme terpisah, sengaja tidak digabung:

1. **`LeadEditableFields.tsx`** — Sumber, Kategori, Temperature, Sudah Survey, Minat Unit/Lokasi, Permintaan, Komentar sekarang bisa diedit lewat tombol Edit → form → **Save**/Batal (beda dari Status/Assign yang auto-save per field). Update `metadata` dengan *merge* (bukan overwrite kolom `metadata` utuh) supaya `origin`/`submitted_at`/dll tidak ikut hilang.
2. **`customer.notes`** (migration `038`) — log **append-only** per lead: waktu + penulis + isi, tidak pernah di-overwrite. RLS `notes_owner_or_admin` ikut visibilitas lead induknya (pola sama seperti `leads_owner_or_admin`), diterapkan sejak migration ditulis — bukan nunggu ketahuan bug `authenticated_all` lagi seperti kasus `035`. GRANT cuma `SELECT`+`INSERT` (tidak ada UPDATE/DELETE dari app, memang dirancang append-only). `author_label` disimpan mentah saat insert (bukan resolve dari `auth_ext.profiles` saat tampil) karena RLS profil cuma izinkan baca punya sendiri — non-admin yang lihat note tulisan admin di lead miliknya tidak akan bisa resolve nama itu lewat query biasa.

**Ini juga fondasi data untuk "Buyer Behavior Intelligence"** (lihat riset di bawah) — sebelum ini, tidak ada satu pun tempat di skema yang mencatat riwayat interaksi dari waktu ke waktu, cuma nilai-terkini di `metadata`.

**Bug ketemu saat tes manual:** `created_by` tidak pernah diisi di `createLead.ts` maupun insert notes baru. Ternyata pola yang sama berulang di **seluruh aplikasi** — `grep created_by` di `src/` nol hasil di kode aplikasi (cuma muncul di tipe TypeScript). Diperbaiki di 2 tempat yang baru ditulis sesi ini; kesenjangan yang sama di tabel/insert lain (mis. `createdBy` saat insert lead dari `/api/leads/intake`, dari service-role — itu memang sengaja NULL karena pemanggilnya mesin, bukan gap) belum diaudit menyeluruh.

## Kecukupan Data untuk "Property Buyer Behavior Intelligence" (riset 26 September 2026)

Ditanya Yohan, dicek ke `project-docs/` (arsip pra-Claude). Temuan:

- Visi asli (Juli 2026): *"Unlike conventional CRM systems, Yohan.AI continuously analyzes **buyer behavior, conversation history, lead quality, and property data**"* — bukan cuma data lead statis.
- Field mentah yang sudah tersimpan sekarang (kategori, permintaan, komentar, minat unit/lokasi, dll) **cukup sebagai bahan baku** — ini persis field yang dulu jadi input AI Processor legacy (`AI_Intent`, `Budget Min/Max`, `Property Match Score`, `Lead Score`, dll, lihat bagian "Rencana Lead Intake" di atas).
- Yang tadinya **belum ada**: riwayat interaksi berwaktu. Sudah ditutup sebagian oleh `customer.notes` (lihat di atas). Yang masih kosong: `chat.conversations`/`chat.messages` (skema ada, data kosong — nunggu keputusan WAHA/Fonnte).

**Kesimpulan:** data mentahnya cukup untuk dianalisis, riwayat perilakunya baru mulai tercatat lewat Notes. Belum ada AI Processor beneran yang mengonsumsi data ini (Follow-up Backlog di dashboard cuma 1 insight sederhana, bukan model AI).

## Export CSV (26 September 2026)

Diminta Yohan: download data CRM ke Excel/spreadsheet. Diputuskan CSV dulu (bukan `.xlsx`) — library `xlsx`/`exceljs` belum ada di project, nambahnya berarti nunggu `npm install` dulu sebelum bisa diverifikasi. CSV zero-dependency, buka langsung di Excel/Google Sheets.

- Tombol "Export CSV" di halaman `/crm`, mengikuti filter aktif (bukan cuma satu halaman/paginasi).
- `getLeads.ts` di-refactor: logika filter diekstrak ke `buildFilteredLeadsQuery()`, dipakai bareng oleh `getLeads()` (paginated) dan `getAllLeadsForExport()` (tanpa batas, di-loop per 1000 baris karena PostgREST punya limit per-request).
- Route `/api/leads/export` session-based (bukan service-role) — RLS `leads_owner_or_admin` otomatis berlaku, non-admin cuma export lead miliknya sendiri.
- **Belum otomatis sinkron ke Google Sheet "Input Data"** — itu permintaan terpisah yang butuh integrasi Google Sheets API (service account, dsb), belum dibangun. Lihat juga bagian "Jalur masuk lead" di atas: aliran data masih satu arah (Form → Database), belum ada arah balik (Database → Sheet).

## Multi-Tenant untuk SaaS — sengaja ditunda

Yohan tanya soal per-user/per-bisnis punya data terpisah (untuk SaaS nanti), dengan admin platform tetap bisa akses semua untuk keperluan support. **Diputuskan ditunda**, bukan lupa — platform masih ON HOLD, baru 2 user di 1 bisnis yang sama. Ini perubahan arsitektur besar (nyentuh RLS di semua tabel domain, bukan cuma `customer.leads`), dikerjakan pas sudah dekat mau onboarding pelanggan pertama. Dicek ke `project-docs/`: multi-tenant memang bagian dari visi awal (`v1.8`: *"single multi-tenant platform capable of serving multiple property agents under one centralized administration"*), tapi **tidak pernah didesain ke skema database** — jadi ini keputusan arsitektur dari nol, bukan tinggal mengaktifkan sesuatu yang sudah direncanakan.

## Sentuhan Warna (26 September 2026)

Ditegur Yohan: platform kelihatan hitam-putih semua. `LeadFunnel`, `PropertySummary`, `AIInsightPanel` sudah berwarna sejak awal; yang genuinely monokrom dan diperbaiki: 4 StatCard dashboard (ikon + warna beda per kartu), badge status pipeline (`qualified`/`proposal`/`negotiation` sebelumnya sama persis warnanya, sekarang progresif dingin→hangat), dan Kategori lead (sekarang badge berwarna, warnanya di-hash dari teks supaya kategori baru otomatis dapat warna stabil tanpa kode disentuh).

## Pencarian & Export Lanjutan (26 September 2026)

Ditanya Yohan cara menyaring lead area Serdam/UNTAN, lalu langsung nanya soal broadcast WA dan download hasil filter:

- **Search diperluas** — field "Cari" (sebelumnya "Cari Nama / No. HP") sekarang ikut cari di `metadata->>minat_unit_lokasi`/`permintaan`/`komentar`, bukan cuma nama/HP. Insight sampingan: warga setempat hampir selalu tulis singkatan lokasi ("Serdam" untuk Sungai Raya Dalam, 75 lead) bukan nama lengkap (cuma 1 lead) -- relevan kalau nanti bikin daftar kata kunci lokasi standar.
- **Search bar dipindah ke baris sendiri** di atas filter lain, dengan tombol kaca pembesar (`Search` icon dari lucide) di kanan input -- submit form GET yang sama, bukan mekanisme terpisah.
- **Export CSV otomatis ikut filter search** -- tidak perlu kode tambahan, `exportParams` di `crm/page.tsx` sudah membawa semua filter (termasuk `search`) sejak fitur export dibuat. Diverifikasi: filter "Serdam" (75 lead) -> export CSV -> 75 baris, cocok persis.
- **Broadcast WA ke hasil filter -- BELUM BISA, dan sengaja tidak dikerjakan.** Ditanya Yohan, dijawab: `getAllLeadsForExport()` yang sudah ada bisa dipakai ulang untuk broadcast begitu WhatsApp tersambung, tapi sesuai `AGENTS.md`, **integrasi WA apa pun tidak boleh mulai dibangun sebelum keputusan WAHA vs Fonnte diambil eksplisit**. Ini murni jawaban konsep di chat, tidak ada kode yang ditulis.

## Bi-lingual ID/EN (26 September 2026)

Diminta Yohan: toggle bahasa di header (ID/EN), nanti dipindah ke Settings kalau halaman itu sudah aktif. Discoping bareng Yohan dulu sebelum mulai (4 pilihan: semua modul sekaligus / toggle+CRM dulu / cuma toggle tanpa isi) -- dipilih **toggle + CRM dulu**, modul lain menyusul bertahap.

- `src/lib/i18n/dictionaries.ts` -- kamus `id`/`en`, dipisah per fitur (`list`/`detail`/`addLead`/`status`). **Nilai DATA (kategori, sumber informasi, temperature, sudah survey) sengaja TIDAK diterjemahkan** -- itu data asli tersimpan di database dalam bahasa Google Form, menerjemahkan tampilannya tanpa menerjemahkan nilai yang difilter/dicari akan bikin mismatch pencarian. Yang diterjemahkan cuma UI chrome dan label status pipeline (`new`/`contacted`/dst -- itu kode internal, bukan data, aman diterjemahkan).
- Locale disimpan di **cookie**, bukan `localStorage` -- semua halaman CRM adalah Server Component yang render teks di server, jadi perlu bisa dibaca server-side. `LanguageToggle.tsx` (client) menulis cookie lalu `router.refresh()`.
- Locale awal dikirim dari server lewat rantai props (`DashboardLayout` baca cookie → `AppShell` → `Header` → `LanguageToggle`), **bukan** dibaca dari `document.cookie` di `useEffect` client -- itu bikin kedipan UI balik ke default sesaat sebelum efek jalan, dan kena lint `react-hooks/set-state-in-effect`.
- **Bug ketemu & diperbaiki:** kamus awalnya punya field berbentuk fungsi (buat interpolasi kalimat seperti "Halaman X dari Y"). Begitu objek kamus lengkap dilewatkan sebagai prop ke Client Component (`LeadStatusSelect` dkk), React gagal serialize ("Functions cannot be passed directly to Client Components"), bikin `/crm/[id]` dan `/crm/new` 500 error. Diganti jadi field string murni, kalimat disusun langsung di komponen yang butuh. **Pelajaran: kamus/dictionary yang dilewatkan sebagai prop ke Client Component harus 100% data serializable (string/number/boolean/object polos) -- jangan taruh fungsi di dalamnya.**

## Prinsip: jangan buang Google Form-nya

Google Form sudah bekerja dan calon lead sudah terbiasa memakainya. Membangun ulang form di dalam platform berarti mengambil risiko tanpa mendapat apa pun — form tetaplah form.

Yang berubah bukan form-nya, melainkan **ke mana datanya pergi**. Sekarang: Form → Spreadsheet, dan spreadsheet itu sumber kebenaran. Nanti: Form → Spreadsheet **dan** → YohanAI, dengan database sebagai sumber kebenaran dan spreadsheet turun pangkat jadi cadangan.

## Tiga bagian yang perlu dibangun

**1. Pintu masuk** — route handler `POST /api/leads/intake`. Menerima JSON dari Apps Script, memvalidasi, menyimpan ke `customer.leads`. Diamankan dengan secret header, bukan sesi login, karena pemanggilnya mesin.

Route ini **wajib ditambahkan ke `PUBLIC_PATHS`** di `src/lib/platform-lock.ts`. Selama platform terkunci, semua route dibelokkan ke `/under-development` — tanpa pengecualian ini, Apps Script hanya akan menerima redirect, bukan respons.

**2. Kurir di Apps Script** — trigger `onFormSubmit` yang memanggil endpoint lewat `UrlFetchApp.fetch()`. Tambahkan dua kolom di spreadsheet: `synced_at` dan `yohanai_lead_id`, supaya baris yang gagal terkirim bisa dilihat dan dikirim ulang, bukan ditebak.

**3. Kunci anti-duplikat** — simpan Form Response ID ke `customer.leads.metadata`, dan tolak bila ID-nya sudah ada. Trigger Apps Script bisa berjalan dua kali saat jaringan bermasalah; tanpa ini satu lead bisa masuk dua kali.

## Urutan yang disarankan

| Fase | Isi | Alasan |
|------|-----|--------|
| 1 | Endpoint + Apps Script + backfill data lama | Spreadsheet tetap jalan. Kalau jembatannya rusak, tidak ada yang hilang |
| 2 | Halaman CRM membaca dan mengubah status lead | Di sinilah mulai didapat yang spreadsheet tidak bisa: pipeline, deteksi nomor ganda, pengingat follow-up |
| 3 | Lepas dari spreadsheet | Hanya setelah benar-benar yakin datanya utuh |

## Yang perlu dipastikan lebih dulu

`lead_sources` belum punya baris untuk Google Form — perlu ditambahkan agar asal lead terlacak rapi.

Dan yang lebih penting: **Apps Script yang ada sekarang bukan sekadar penangan form.** Menurut `docs/migration/migration-blueprint.mdx`, di dalamnya ada AI Processor, Decision Engine, CRM Engine, serta integrasi WAHA/n8n. Sebelum menulis kode apa pun, baca dulu isi Apps Script-nya dan petakan mana yang sekadar memindahkan data dan mana yang mengandung logika bisnis — bagian kedua itu tidak boleh ikut dipindahkan secara membabi buta.

**Halaman `/crm` masih placeholder** berisi empty state, belum tersambung ke database.

---

# Objectives

CRM membantu agen untuk:

- Mengelola Lead
- Mengelola Customer
- Mencatat aktivitas
- Menjadwalkan Follow-up
- Memprioritaskan prospek menggunakan AI

---

# Current Features

## Lead List

✅ **Selesai (Fase 2).** Menampilkan lead dari database, dengan filter tanggal/kategori/temperature, pagination, dan tombol kontak WhatsApp langsung. Lihat `src/app/(dashboard)/crm/page.tsx`.

**Pencarian diperluas (26 September 2026):** field "Cari Nama / No. HP" tadinya cuma cari di `first_name`/`last_name`/`phone`. Ditanya Yohan cara saring lead berdasar kata kunci lokasi (mis. "UNTAN", "Serdam"), diperluas ikut cari di `metadata->>minat_unit_lokasi`, `permintaan`, `komentar` — field teks bebas tempat kata kunci lokasi/kebutuhan biasanya muncul, bukan di kolom terstruktur. Ketemu insight data sambil verifikasi: warga setempat hampir selalu pakai singkatan lokasi ("Serdam" untuk Sungai Raya Dalam, 75 lead) alih-alih nama lengkap (cuma 1 lead) — relevan kalau nanti mau bikin daftar kata kunci lokasi standar.

---

## Lead Detail

✅ **Selesai (Fase 2, 24-26 September 2026).** Halaman `/crm/[id]` — semua field lead + metadata (permintaan, komentar, minat unit/lokasi, temperature awal, survey, follow-up) tampil lengkap, tombol WA, link balik ke Lead List. Ubah status (dropdown pipeline) dan assign ke agent (khusus admin/super_admin) bisa langsung dari halaman ini. Sejak 26 September: field Sumber/Kategori/Temperature/Sudah Survey/Minat Unit/Permintaan/Komentar bisa diedit (tombol Edit → Save), plus riwayat catatan berwaktu (lihat "Notes" di bawah), plus **Percakapan WhatsApp** (lihat di bawah). Lihat `src/app/(dashboard)/crm/[id]/page.tsx`.

---

## Percakapan WhatsApp

✅ **Selesai (26 September 2026).** Section baru di Lead Detail — kirim & terima pesan WhatsApp langsung dari CRM, live lewat Supabase Realtime (tidak perlu refresh manual). Bagian dari integrasi Kapso (Communication Automation), lihat `docs/status.mdx` Task 008 untuk detail arsitektur lengkap. Pesan media (foto/video) tampil sebagai notice "buka WhatsApp untuk melihat", bukan link storage mentah. Komponen: `src/components/crm/LeadWhatsApp.tsx`.

---

## Tambah Lead Manual

✅ **Selesai (26 September 2026).** Halaman `/crm/new` — form input lead langsung dari dashboard (telepon, walk-in, di luar Google Form), field & pilihan sama seperti Google Form. Diakses dari tombol "Add Lead" di dashboard dan "+ Tambah Lead" di halaman CRM. Lihat `docs/modules/crm.mdx` bagian "Tambah Lead Manual" untuk detail.

---

## Timeline

🟡 **Sebagian.** Notes (lihat di bawah) sudah jadi bentuk timeline paling dasar. Belum ada tipe aktivitas terstruktur (Chat/Call/Meeting/Visit sebagai kategori tersendiri, bukan teks bebas).

---

## Activity

🔵 Belum dikerjakan. Seluruh aktivitas agen terhadap lead, terkategori (di luar Notes yang bebas teks).

---

## Notes

✅ **Selesai (26 September 2026).** Tabel `customer.notes` (migration `038`), append-only — waktu + penulis + isi, tidak pernah di-overwrite. Ditambahkan dari Lead Detail, tampil terurut terbaru dulu. Lihat `src/components/crm/LeadNotes.tsx`.

---

## Follow-up

Penjadwalan aktivitas berikutnya.

---

# Future AI Features

CRM Intelligence akan memberikan:

- Lead Score
- Buyer Intent
- Buying Signal
- Priority Recommendation
- Next Best Action
- Conversion Prediction

---

# User Flow

```text
Lead Masuk

↓

CRM

↓

Lead Detail

↓

Timeline

↓

AI Recommendation

↓

Follow-up

↓

Deal / Lost
```

---

# Folder Structure

```text
src/

app/crm/

components/crm/

lib/crm/

types/
```

---

# Database

Primary Tables (ada, terverifikasi langsung ke database)

- customer.leads
- customer.lead_sources
- customer.contacts (skema ada, belum dipakai kode aplikasi manapun)
- customer.notes (sejak migration `038`, 26 September 2026)

Future

- customer.activities (Activity terstruktur, lihat "Current Features" di atas -- beda dari customer.notes yang sudah ada)
- ai.lead_scores
- ai.recommendations

---

# Business Rules

- Satu Lead memiliki banyak Activity.
- Satu Lead memiliki banyak Notes.
- Semua perubahan dicatat pada Timeline.
- Follow-up wajib memiliki tanggal.

---

# Dependencies

- Authentication
- Dashboard
- Communication
- Sales
- AI Intelligence

---

# Current Limitations

- Belum ada UI bulk-assign (assign banyak lead sekaligus dari Lead List) — cuma satu-satu dari Lead Detail
- Timeline/Activity belum lengkap — Notes (append-only, bebas teks) sudah ada, Activity terstruktur (Call/Meeting/Visit sebagai kategori) belum
- Belum ada sinkronisasi balik ke Google Sheet "Input Data" — lead manual & perubahan dari dashboard tidak tercermin di spreadsheet
- Export CSV belum jadi `.xlsx` asli (belum ada dependency `xlsx`/`exceljs` di project)
- Multi-tenant (data terpisah per bisnis untuk SaaS) sengaja ditunda — lihat bagian "Multi-Tenant untuk SaaS" di atas
- AI baru sebagian aktif (1 insight nyata: Follow-up Backlog)
- Automation belum aktif

---

# Future Roadmap

## Phase 1

CRM Foundation

## Phase 2

Timeline

## Phase 3

Activity Management

## Phase 4

AI Lead Intelligence

## Phase 5

Automation

---

# Related Documents

- Project Context
- Project Status
- Database Schema
- Dashboard Module

---

# Last Updated

26 September 2026

Sprint 011 — Platform Hold